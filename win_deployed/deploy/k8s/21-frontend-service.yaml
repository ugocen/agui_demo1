apiVersion: v1
kind: Service
metadata:
  name: agui-frontend
  namespace: agui
  labels:
    app.kubernetes.io/name: agui-frontend
    app.kubernetes.io/part-of: agui
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agui-frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
