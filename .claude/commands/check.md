---
description: Check environment prerequisites (Python, Node, npm, Docker, project setup)
---

Check the environment is ready for Phase 0 work:

1. Confirm Python 3.13, Node, npm, `uv`, and `git` are installed. Docker is
   optional (not required for local Phase 0 dev).
2. Confirm project setup: `Phase0/.env` exists (copy from
   `Phase0/.env.example` if not), `Phase0/backend/.venv` exists,
   `Phase0/frontend/node_modules` exists, and ports 8000/3000 are free.

Summarize ok/warn/fail per item and, for any failure, give the exact fix
command.
